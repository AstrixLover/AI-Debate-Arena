That 404 Not Found happens because Replit Agent sometimes creates the files but doesn't actually start the Streamlit server, or it's trying to host the app on the wrong port. Since your file structure shows main.py is exactly where it should be, we can fix this in the second prompt by forcing it to configure the entry point correctly.

Here is the Second Mega-Prompt. It includes the fix for the 404 error and injects the heavy AI logic we discussed.

The Final "Fix & Brain" Prompt
Copy and paste this into Replit Agent:

The file structure looks correct, but I am getting a 404 error in the preview. We need to fix the deployment and fully implement the AI logic now.

1. Fix the 404 Error:

Ensure the Replit .replit file or the run command is strictly set to streamlit run main.py --server.port 5000 --server.address 0.0.0.0.

If you created a pyproject.toml or replit.nix, ensure streamlit is the primary entry point.

2. Implement the AI Functions in main.py:

Search Logic: Use tavily-python. Search the 'Debate Topic' and pass the top 3 results to Gemini 2.5 Flash. Gemini must generate two distinct "Tactical Briefs"—one for each persona—based on the real-world facts found.

The Debate Loop: >    - For each round, the active Debater must first perform a "Hidden Thought" step: 'Based on my persona and the facts, what is my draft? Now, what are the flaws in that draft? Rewrite it to be stronger.'

Only the final rewritten version should be streamed to the UI using st.write_stream.

Maintain the full conversation history in st.session_state.chat_history.

The Judge: At the end, Gemini must analyze the whole chat and return a strict JSON object (use response_mime_type: "application/json") with: Winner, Score_A, Score_B, Fatal_Flaw, and Verdict.

3. Persona System Prompts:
Implement the simplified archetypes we discussed: The Scholar, The Joker, The Robot, The Lawyer, The Dreamer, The Skeptic. Hardcode their specific "voice" instructions into a dictionary.

4. Reliability:

Use try-except blocks around the Tavily and Gemini calls.

If an API key is missing from Secrets, display a clear st.error message instead of crashing.

Update main.py now with the complete, functional code and ensure the preview works.